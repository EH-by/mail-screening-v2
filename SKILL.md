---
name: mail-screening-v2
description: 当用户提到"群发邮件""发送邮件""邮件筛选""筛选报名表""报名数据处理""纳新筛选""筛选候选人""报名表清洗""名单分组""录取名单""候补名单""活动报名""邮件正文""写邮件""邀请邮件""通知邮件""致谢邮件""群通知""截止提醒"等意图，或上传报名信息表格（Excel/CSV/粘贴 CSV 文本，支持多文件分批次上传；兼容腾讯问卷等平台导出格式：序号前缀表头、提交答卷时间、微信列、平台元数据列）要求按标准筛选分组、生成邮件正文时使用。流程：多文件合并→自动清洗→清洗摘要确认→选择题式一轮 grilling（Q0–Q4：选项来自表格数据 + 邮件要素收集）→统一执行（去重/邮箱补齐/筛选/录取-候补-致谢判定/分组）→统一输出（对话简报 + 筛选结果-{活动名}-{日期}.txt 工作日志，含三组名单、英文分号收件人串与邮件要素区）→确认后直接生成各组邮件正文与群通知（对话 Markdown 交付，含主题行）。组织身份以文首「唯一配置区」为准（默认 TIDE Club，换组织只需修改该区）。公众号/海报/朋友圈/双语/多渠道内容包不属本技能范围。
---

# 邮件筛选 v2.0（报名筛选 + 邮件正文一体交付）

报名表清洗、筛选与分组，输出分组名单、收件人串与各组邮件正文（对话 Markdown 交付）。

## ■ 唯一配置区（换组织只需改本区；全文组织相关引用一律以本区为准；本区值为规则默认值，非示例）

- 组织名称（简称）：健身社
- 组织全称：FITNESS CLUB
- 默认邮箱域名：link.cuhk.edu.cn（缺失邮箱推导格式 `{学号}@{域名}`）
- 落款署名：健身社
- 组织定位理念：建立健身兴趣社群，推广健康健身理念；简单健身动作教学、健身计划制定
- 语气基线：社群的友善、亲切 + 学生组织的亲和力 + 项目实践的行动导向
- 常用术语（保留英文，首次面向非技术受众补简短中文解释）：无
- 视觉配色 Token（仅 DOCX/PDF/HTML 交付物使用；对话 Markdown 不伪造颜色）：主色 `#102A43`（主标题/横栏/一级标题）｜强调色 `#1F6F9F`（二级标题/链接/行动提示）｜辅助强调 `#2FA7C7`（小面积标签/分隔线）｜信息区背景 `#EEF5F8`｜卡片背景 `#F8FBFC`｜正文 `#243B53`｜次要文字 `#627D98`
- 换组织注意：改写本区后，§13.5 中带组织色彩的特化措辞须按新组织重新表述（如新组织无"教授指导"定位，须删除，不得照搬）；视觉系统整体替换，不得混用新旧配色；「组织定位理念/语气基线/常用术语」若新组织不适用可直接改写

## 0. 定位与四条铁律

- **P1 一轮 grilling（选择题式）**：只问一轮必要问题（Q0–Q4），问完即停；选项取值优先从表格数据提取，用户只输数字/字母。为什么：最小化用户阅读与输入，规则语义不漂移。
- **P2 只存储、不产出**：grilling 阶段只收集并存储规则（会话内标准，不生成文件）；不展示任何处理结果（分组/判定/收件人串）。唯一例外：清洗摘要（数据事实，供 Q0 选择题确认）。为什么：避免"预览—修改—再预览"循环，反而更慢。
- **P3 内置规则库**：筛选规则与判定规则内置常见类型/模式（见 §10），用户选择题型 + 填参数；同时支持自然语言自定义（类型5 / 模式D）；邮件内容类型见 §13。为什么：选择题锁定规则，零歧义。
- **P4 筛选 + 邮件一体闭环**：本技能收敛于筛选+邮件一体交付——确认分组后直接基于结果文档与 Q4 邮件要素生成各组邮件正文与群通知（对话 Markdown）；输出即交付，不再交接独立邮件技能；公众号/海报/朋友圈/双语/多渠道内容包不在范围。为什么：筛选与邮件共享同一份标准与要素，一体完成，避免跨技能交接的成本与标准漂移。
- **组织引用纪律**：全文所有组织身份、定位、语气、术语、配色与域名均以文首「唯一配置区」为准；规则正文不得写死组织名称。

## 1. 总流程（六阶段一览）

| 阶段 | 动作 | 产物 | 是否对用户展示 |
|---|---|---|---|
| ① 开场说明 | 一句话介绍 + 上传要求 | 无 | ✅ |
| ② 接收与清洗 | 接收校验 + 自动清洗 | 清洗摘要（仅展示） | ✅（仅摘要） |
| ③ grilling | 选择题收集规则 + Q4 邮件要素 + 存储 | 筛选标准 + 邮件要素（会话内，不落盘） | ✅（问答） |
| ④ 统一执行 | 完整处理管道 | 无（中间态不外显） | ❌ |
| ⑤ 统一输出 | 生成结果文档 | 筛选结果-{活动名}-{日期}.txt + 简报（环境无法输出文件时降级为对话 Markdown，见 §7.4） | ✅ |
| ⑥ 确认与交付 | 人审 → 逐组生成邮件正文 | 各组邮件正文（对话 Markdown）+ 内部区块 | ✅ |

## 2. 强制要求（核心防线，任何版本不可移除）

1. 收件人串、抄送串一律用英文分号 `;`（不是中文分号 `；`），不加空格，结尾无多余分号。为什么：中文分号 Outlook 不识别，会全员收不到。
2. 宁可在异常清单中报告，绝不静默丢弃或擅自修改数据。为什么：静默丢弃 = 事后"某人凭空消失"无法追责。
3. 所有统计数字必须与名单逐条对应，可复现。为什么：审核从"逐条数"降级为"看摘要对总数"。
4. 姓名、学号、邮箱、联系方式等字段原样保留（除首尾空格与格式修正外），不得改写。为什么：改写即失真，核对无从谈起。
5. 只输出规格规定的 markdown 格式 txt 文件本身，不加额外解释文字；**环境无法输出文件时降级为对话 Markdown 完整交付（§7.4），绝不静默放弃**。为什么：输出即交付物，无需二次加工；交付优先级 = 内容完整 > 落盘形式。
6. 邮件正文一切事实必须来自 Q4/用户提供，缺失进「待确认」，绝不编造补全。为什么：编造时间/地点/链接 = 参会者跑错场、错过截止。
7. 邮件正文以对话 Markdown 交付（含主题行），用户明确要文件才生成；文件动作在「状态检查」如实标注。为什么：交付即复制可用，不产生未请求的文件。

## 3. 阶段一：开场说明（极简版）

**触发**：用户提到 群发邮件 / 发送邮件 / 邮件筛选 / 筛选报名表 / 报名数据处理 / 纳新筛选 / 筛选候选人 / 报名表清洗 / 名单分组 / 录取名单 / 候补名单 / 活动报名 / 邮件正文 / 写邮件 / 邀请邮件 / 通知邮件 / 致谢邮件 / 群通知 / 截止提醒，或上传报名表格要求筛选分组，或显式调用 `/邮件筛选`。

**开场文案（仅当用户未附带表格时输出，逐字）**：

> **【邮件筛选】** 报名表清洗、筛选与分组，输出分组名单、收件人串与各组邮件正文（对话 Markdown 交付）。
> 请上传报名表格（Excel 优先；解析失败时另存为 CSV；可分批次上传多个文件，会自动合并）。

**自适应**：
- 调用与上传同时发生（用户已附带表格）→ **跳过开场说明**，直接进入阶段二。
- 用户未上传 → 输出上述一句话后等待，不开始任何处理。
- 用户未上传且意图是纯邮件/群通知写作（无筛选需求）→ 降级「仅邮件写作」模式：输出一句"无报名表时按仅邮件写作处理"，跳过阶段二至五，直接进入 §13（要素收集沿用 Q4 的 a–g 问题形式）。

## 4. 阶段二：接收与自动清洗

### 4.1 接收

| 输入形式 | 处理 |
|---|---|
| 单文件 `.xlsx` | 直接进入清洗 |
| **多文件（分批次收集）** | 合并：表头对齐后纵向拼接；列结构不一致时缺失列留空，并在摘要中注明列差异 |
| `.csv` 兜底 | 解析失败提示"另存为 CSV"；警告 CSV 三类风险（日期文本化/字段内逗号/BOM 乱码），发现即记入异常清单（文件级条目，见 §7.3） |
| 粘贴文本（CSV 样式） | 与文件同流程；必须有表头，否则视为无表头报错 |

部分文件解析失败而部分成功 → 成功者照常处理，失败文件在摘要与异常清单注明，不整体中断。

### 4.2 自动清洗（此时执行，结果仅用于展示确认）

> **不预设表格模板**：输入表的形态千差万别——问卷平台导出（腾讯问卷/问卷星/金数据等，带序号前缀表头、提交答卷时间、平台元数据列、多选拆列、数字编码列）、手工整理表（列名自由、无元数据列）、粘贴文本等。**永远以每次实际执行的「表头识别 + 可筛列提取」结果为准**；references 中的样本示例仅为演示语法，不代表常见格式，不得按样本样式反向推断真实表结构。

0. **合并单元格预处理**（仅 Excel；CSV/粘贴文本无合并单元格概念，跳过）。为什么：合并区域的值只存在于左上角单元格，不处理会导致字段漏读、行误判为空、表头识别错位：
   a. 用读取工具（如 openpyxl 的 `merged_cells.ranges`）获取全部合并区域，将每个区域左上角的值**复制填充**到区域内所有单元格，填充后与普通单元格无差别地参与后续步骤。
   b. **表头行探测**：表头可能不是第 1 行（平台导出常见两行表头：第 1 行是合并大标题如"健身社报名表"，第 2 行才是列名）。探测规则：取前 5 行中"命中别名关键词数量最多且 ≥1"的行作为表头行；更靠前的行若填充后仅 1 个非空单元格且不含别名关键词 → 判定为标题行，跳过并在摘要注明"多行表头：标题行已跳过"。表头行上方内容一律不进入数据。
   c. **数据区合并**（列内多行合并）：填充后正常参与清洗/去重/筛选，不做特殊处理，不再视为缺值。
   d. 摘要注明："检测到合并单元格 N 处，已按合并区域填充"。
   e. 多 sheet 场景下**每个 sheet 独立执行本预处理**，再进入多 sheet 选择与合并。
1. **表头识别**（按顺序执行，先判定者短路）：
   a. **剥离序号前缀**：行首 `数字 + 、/. ．`（如 `1、姓名`、`3.学号`）先剥离。
   b. **多选拆列识别（先于别名匹配）**：同一题拆分的多列（同序号前缀的多列，或列名主体相同、仅括号选项后缀不同，如 `N、{题目}（多选）({选项一}…)` 与 `N、({选项二}…)`【此为例】）→ 识别为同一题的多选项列，摘要注明"第 N 题为多选，拆为 2 列"，**不再参与别名匹配**，不进可筛列。为什么：多选拆列的选项名常含关键词（如选项名带"书院""学院"等字样），若先做别名匹配会被误识别为具名列。
   c. **平台元数据列识别**（序号 / 所用时间 / 来源 / 来源详情 / 来自IP 等问卷平台导出列）→ 跳过，摘要注明"平台元数据列已跳过"，不参与清洗与筛选。
   d. **别名匹配**（子串匹配，忽略大小写与首尾空格），识别不到的列保留原样，摘要中说明"未识别列"。**同一类型多列命中的消歧**：① 与别名全等者优先；② 无全等 → 取列名最短、且不含"意向/预期/期望"等修饰词者（如「性别」与含"意向"修饰的列并存时，仅「性别」归入性别列）；③ 仍并列 → 都不归入。未归入的命中列保留为普通列（可进可筛列），摘要注明消歧结果。别名表：
   - 姓名：姓名 / 名字 / 中文名 / Name / Full name / Student Name
   - 学号：学号 / Student ID / 证件号
   - 邮箱：邮箱 / 电子邮件 / Email / E-mail
   - 报名时间：报名时间 / 提交时间 / 填报时间 / 注册时间 / created_at / 提交答卷时间 / 答卷时间 / 提交问卷时间
   - 身份类型：身份 / 身份类型 / 本科生 / 研究生
   - 年级：年级 / 入学年份 / Year
   - 学院：学院 / Faculty / School
   - 书院：书院 / College
   - 专业：专业 / Major / Programme
   - 联系方式：联系方式 / 手机 / 手机号 / 电话 / Phone / 微信 / 微信号 / WeChat
   - 性别：性别 / Gender
2. **去重**：同一学号 / 同一邮箱 / 同一微信（微信号或手机号）多条 → 保留【最后一次提交】（按报名时间列最晚者；该列缺失/无法解析时退化为表格行序，靠后行视为最后一次）。被去重条目计入"重复报名"。摘要必须注明本次采用的依据（报名时间列/行序）。
3. **清理**：删除全空行；去除单元格首尾空格；日期统一 YYYY-MM-DD（识别四种形态：Excel 序列号 46215.03【此为例】/ 中文日期 2024年9月1日【此为例】/ ISO 2024-09-01【此为例】/ 平台导出 2025/11/10 19:12:12【此为例】即 `YYYY/MM/DD HH:MM:SS`）。含时间信息时保留至秒（排序用）。
4. **校验**：邮箱格式必须匹配 xxx@yyy.zzz；姓名/学号任一缺失 → 记入异常清单（记录保留，正常参与后续流程）。邮箱列缺失或为微信列 → 不视为异常（走 §6.2 邮箱补齐）。
5. **提取选项值**：为 grilling 选择题准备——提取**可筛列**的唯一值列表及各自人数（如"性别：男(30)/女(18)"【此为例】），以 `a/b/c…` 字母编号，供 Q1 选项使用。**可筛列 = 表中所有列（含未识别列，只要取值种类合适即可入选）中取值种类 ≥2 且 ≤10 的列**，排除：已识别关键列（姓名/学号/邮箱/联系方式等）、平台元数据列、报名时间列、多选拆列。取值全为数字编码（如 1/2/3）的列标注"（编码）"（多选拆列虽是 0/1 编码，但已在步骤 1b 判定并排除，不属此类）。**注意：必须在 Q0 确认最终数据之后才计算选项值**（多 sheet 选择可能改变数据）。

### 4.3 前置校验失败（唯一允许提前报错）

文件打不开 / 完全无表头 / 空文件（0 行有效记录）/ 所有文件均无法解析 → 立即报错请用户重新上传（后续全部依赖数据）。

## 5. 阶段三：grilling 第一轮（选择题式）

### 5.1 原则

- 只问 5 个问题（Q0–Q4），顺序固定，一轮内完成，问完即停（P1）。
- 最小输入：用户只需输入数字序号 / 简短参数；选项取值优先来自表格数据（§4.2.5 提取的唯一值列表）。
- 每个问题均支持"自定义/自然语言"通道（口述新规则）。
- 用户答"随便 / 按默认 / 不答" → 使用 §9 缺省值表，**绝不追问**；缺省值逐题判定——命中当前题的缺省即用，其余题继续按顺序问完。
- 用户拖入「筛选标准.md」→ Q1/Q2 以其内容为准（视为已答），Q0 照常问（清洗摘要是数据事实，与规则无关），Q3 按 §5.5 处理（自动重置），Q4 照常问（邮件要素是事实，与规则无关）。
- **全程不展示处理结果**（P2）：不预览分组、不报判定人数、不展示收件人串。

### 5.2 Q0 · 清洗摘要确认（模板逐字，数值【此为例】）

```
已读取并清洗（共 87 行；多文件已合并：A 文件 58 行 + B 文件 29 行）【此为例】：
- 识别列：姓名 / 学号 / 邮箱 / 报名时间 / 身份 / 书院 / 学院（未识别 2 列：备注、测试）
- 数据问题：邮箱格式错误 3 处 ｜ 报名时间无法解析 2 处 ｜ 重复报名 4 条
请确认（输入序号）：
1. 无误，继续　　2. 忽略"测试"列　　3. 其他调整（口述）
```

- **多 sheet**：追加一行（独立编号，不并入主选项）："工作簿含多个 sheet，使用哪个？（1. 正式报名 2. 测试数据 3. 合并全部）"；缺省 = 第一个含数据（≥1 行有效记录）的 sheet，摘要注明。
- 用户选 2/3（含 sheet 行）→ 重新清洗并更新摘要，**不再要求再次确认 Q0，直接进入 Q1**（防循环）。选项值提取基于更新后的数据。
- 多文件合并时摘要注明列差异（§4.1）。
- 检测到合并单元格/多行表头时，在"数据问题"行后追加注明（如"合并单元格 3 处，已填充 ｜ 多行表头：标题行已跳过"）。

### 5.3 Q1 · 筛选规则（选择题，选项来自表格；模板，数值【此为例】）

```
请选择筛选方式（可组合，如"2b+3a"；或选 5 直接口述自定义规则）：
1. 按报名时间先后（先到先得）
2. 按列取值筛选——表中可筛列：a.性别[男30/女18] b.{活动专属列}[1:33/2:15(编码)]【此为例】，
   输入列字母（如 2b = 选 b 列）；随后我会列出该列取值，你输入保留取值的字母
3. 按列优先级——从可筛列中选列（如 3a = 选 a 列），
   随后输入该列取值的优先顺序字母（如 b,a；单个字母 = 仅该项优先，其余按默认排序规则）
4. 组合筛选（如"2b+3a" = 先按 b 列筛选、再按 a 列优先级）
5. 自定义（自然语言描述新规则）

（也可直接口述筛选规则，如"保留女生""先按某列筛保留女、再按性别优先"）
```

**输入方式（两条通道，等价，任选其一）**：
- **格式输入**：数字 + 字母（最小输入，如 `2b`、`3a b,a`），解析规则见下。
- **自然语言输入**：任何一句自然语言（如"保留女生""先把研究生的排前面""先按某列筛保留女、再按性别优先"），按语义映射规则见下。不同用户的说法千差万别，**先识别意图再落参数，不要求用户按格式说话**。

**格式输入解析规则（确定性算法）**：
1. 归一化：去除空白与 `、，；,;`，保留数字、字母、`+`。
2. 连续数字段 = 类型选择，**按位拆分**，"23" 与 "2+3" 等价，执行顺序 = 数字出现顺序。
3. 连续字母段 = 参数：**第一个字母 = 所选列的字母**（如 "2b" = 类型2 选 b 列；"3a" = 类型3 选 a 列）；该列的取值字母在下一步追问中收集，不在此步解析。
4. **答案含 5（自定义）** → 除 5 以外的整段文本视为自定义规则原文，不再拆分字母参数。
5. **纯字母无数字** → 默认解析为类型3（按列优先级）：第一个字母为列，其余字母为该列取值优先级。
6. 选列后**在同一轮内追问一次**该列的取值选项（如"b 列（{列名}）取值：a.1[33] b.2[15]（编码，含义？）请输入保留/优先的字母；剔除用 `!` 前缀（如 `!a` = 剔除 a，保留其余）；可顺带口述取值含义"）。类型2 支持"保留"与"剔除"两方向：自然语言"剔除 X"与格式 `!a` 等价，方向与取值字母一并记入规则参数原文。编码列的取值含义由用户口述（如"1={含义}"），**记入规则参数原文**，供结果文档"判定理由"引用。
7. 参数缺失（选 2 未选列、选列后未选取值等）→ 同一轮内追问一次 → 再不答用缺省值（类型2 = 全员保留并注明；类型3 = 仅按时间排序并注明）。
8. 表中无可筛列 → 类型2/3 选项标注"（表中无可筛列，不可选）"。

**自然语言输入解析规则（语义映射 + 回显兜底）**：
1. **先识别意图要素，再落类型与参数**。常见语义要素 → 类型映射：
   | 用户说了什么（示例） | 映射 |
   |---|---|
   | 只要 / 保留 / 只留 / 要…的 | 类型2（按列取值筛选，保留指定取值） |
   | 剔除 / 不要 / 筛掉 / 排除 / 去掉… | 类型2（剔除指定取值，保留其余） |
   | 优先 / 先考虑 / 排前面 / 先…后… | 类型3（按列优先级） |
   | 先…再… / 然后 / 接着 | 类型4（组合筛选，顺序 = 句子顺序） |
   | 先到先得 / 报名早的 / 按报名顺序 / 时间先后 | 类型1（按报名时间先后） |
   | 其余无法归类的描述 | 类型5（自定义，原样采纳回存） |
2. **句子中的列名/取值 → 与清洗提取的可筛列及取值列表做匹配**（子串/同义匹配："女生"→性别列取值"女"；"研究生"→身份列"研究生"；数字编码→需与用户确认含义）。匹配不到 → 归入自定义规则原文并回显。
3. 句子中出现的数字编码含义（如"1=男"【此为例】）→ 记入规则参数原文。
4. 混输（如"选2保留女"）：数字段按格式规则解析类型，文本段解析该类型参数。
5. **跨题识别**：自然语言中若含判定类内容（"录取 X 个""候补 X 个""全员录取""分 X 组通知"），识别为 Q2 的答案，Q2 时直接采用并回显，不重复询问；其余部分按筛选规则解析。
6. **无论哪种通道、何种解析，完成后必须用一句话回显复述**（"已记录：类型2 性别=保留 女；类型3 {列名}优先 1→3"），用户确认或纠正——回显是防语义漂移的最终兜底；这是同一轮内的澄清，不是新问题。

- 组合筛选（类型4）：多个类型按"用户声明顺序"（格式输入 = 数字出现顺序；自然语言 = 句子顺序）先后执行，前者筛除者不进入后者；类型1 无筛除作用，仅贡献最终时间排序。摘要按序记录执行链（如"类型2 → 类型3 → 时间排序"）。
- 可筛列定义见 §4.2.5；0/1 多选拆列、平台元数据列、报名时间列不进可筛列。
- 解析示例与反例见 references/输入解析与边界示例.md。

### 5.4 Q2 · 状态判定规则（选择题；模板逐字）

```
请选择录取模式：
A. 名额制：录取___人、候补___人（只填数字）　B. 全员录取　C. 仅通知不录取（分组数___）　D. 自定义（口述）
排序依据：1. 报名时间（默认）　2. 按列优先级（复用 Q1 类型3 所选列）　3. 自定义
```

**模式参数解析**：模式字母 + 数字段（模式A 前两个数字 = 录取 N、候补 M；只给一个 → M=0）+ 末尾数字 = 排序依据编号（缺省 1）。模式C 取一个数字为分组数。模式D 采纳口述全文（若口述已含排序语义，跳过排序依据行）。

**排序依据规则**：
- 未指定排序依据 → 默认按报名时间；时间并列 → 学号升序（tie-breaker，见 §6.4）。
- 选 2（按列优先级）→ **直接复用 Q1 类型3 已录的列与字母链，不重问**；Q1 未设置类型3 → 该选项标注"（Q1 未设置该优先级，不可选）"。
- 排序依据支持组合（如"1+2"），tie-breaker 学号升序始终在最后一级。
- 类型1（先到先得）与 Q2 排序依据冲突（选 1 且排序依据≠时间）→ 以 Q2 明确选择为准，输出注明"类型1 先到先得与排序依据冲突，按排序依据执行"。
- 名额未满/超出 → 摘要显式说明（"合格 18 人 < 录取 30 人【此为例】，按实际录取"）。

### 5.5 Q3 · 筛选标准（会话内重建，不落盘）

- 筛选标准是**会话内标准，不生成任何文件**（不导出筛选标准.md，不要求对标准做文件级确认）。每次运行都按 **本次文件内容 + 本次 Q1/Q2 回答在会话内重建**——每份表格、每个活动标准各不相同，**绝不沿用上次标准**；防规则串届由此机制直接保证，无需用户确认。
- 标准内容 = 本次 Q1 筛选规则原文 + 本次 Q2 状态判定规则原文 + 活动名称；规则原文随结果文档「1. 统计摘要」落盘（供邮件正文生成与追溯使用）。
- 只问一句：**"活动名称？（可选，用于结果命名）"**→ 用于结果文件命名；不答/随便 → 缺省仅用日期命名。
- 不输出"已保存/以后自动复用"类话术；结果文档「规则来源」标注"本次设置"或"使用了缺省值"。

### 5.6 Q4 · 邮件要素收集（与 Q0–Q3 同一轮，模板逐字）

**原则**：收集邮件正文所需事实，形成完整文件（结果文档「6. 邮件要素」）；与 Q0–Q3 同一轮（P1），问完即停，不另起一轮。**无需邮件写作时，本问题答"无"或不填写答案（见规则），不收集要素、不生成邮件。**

```
Q4 · 邮件要素（如无需邮件写作，本问题请填写"无"或不填写答案；否则每项直接输入内容，或回"缺"跳过；可整体答"随便/按默认"）
请提供邮件正文所需要素（只填你有的项）：
a. 活动时间：______（如"9月5日 周六 14:00-16:00"【此为例】，含开始/结束）
b. 活动地点：______（线下地址或线上链接）
c. 主题一句话：______（缺省用活动名称）
d. 报名链接：______（必须完整链接）
e. 报名截止：______（如"9月3日 23:59"【此为例】）
f. 联系人：______（姓名 + 联系方式）
g. 抄送：______（1. 无　2. 输入邮箱串　3. 分组填写：录取/候补/致谢）
```

**输入方式（两条通道，等价）**：
- **逐项输入**：按 a–g 逐项填内容；某项答"缺"即跳过；**整题答"无"即跳过全部要素**。
- **自然语言口述**：一句口述（如"周六下午两点，教102，主题是 AI 分享，链接我发你，截止周五晚，联系人李四"）→ 按语义拆入各项；不同用户的说法千差万别，**先识别要素再落项，不要求按格式说话**。

**规则**：
- **无需邮件写作**：答"无"或整题未填 → 本次不需要邮件写作：跳过邮件要素收集，不逐项询问、未填项不列入待确认；回显"已记录：本次不生成邮件正文，仅交付筛选结果"。
- **回显兜底**：收集完毕一句话回显（"已记录：时间 周六 14:00-16:00 ｜ 地点 教102 ｜ 主题 AI 分享 ｜ 未提供：报名链接、报名截止、联系人 → 列入待确认"），用户确认或纠正——与 Q1/Q2 回显同级。
- **跨题识别**：grilling 其他问题中已出现的邮件要素（时间/地点/链接/截止/联系人）自动归入 Q4 对应项，Q4 直接采用并回显，不重复询问。
- **不询问项**：组织名称（以「唯一配置区」为准；用户口述其他组织时以用户为准）；邮箱域名（沿用 §5.7 已确认值，Q4 不重复）。
- 表中若存在明显的时间/地点/主题类列，其唯一值可作为 Q4 选项建议（标注【此为例】），不强制。
- 缺失要素（回"缺"或未答）→ 按 §9 缺省值表：标记待确认，不编造、不追问。

### 5.7 存储动作（会话内存，不落盘）

grilling 结束后将本次标准存入会话内存（P2：存储而非产出）：筛选规则原文、状态判定规则原文、活动名称、**邮件写作需求（需要 / 不需要）**、**邮件要素（活动时间/地点/主题一句话/报名链接/报名截止/联系人/抄送；不需要邮件写作时为空）**。**不生成、不修改任何文件**；规则原文随结果文档「1. 统计摘要」落盘、邮件要素随「6. 邮件要素」落盘，供阶段六生成邮件正文与后续追溯使用。

组织信息以「唯一配置区」为准（用户口述其他组织/域名时以用户为准）；邮箱推导域名缺省取配置区「默认邮箱域名」，用户口述说明时以用户为准，结果文档标注本次所用域名。

## 6. 阶段四：统一执行（处理管道）

收到全部规则与数据后**一次性**完成，不向用户展示任何中间态（P2）。唯一例外：数据读取失败（§4.3）。

### 6.1 清洗（复用阶段二结果）

阶段二已完成：表头识别、去重、清理、日期归一、邮箱校验、选项值提取。此处直接使用，不再重复。

### 6.2 邮箱补齐

1. 无邮箱但学号为有效数字（学号列值仅由数字组成，允许首尾空格）→ 推导 `{学号}@{域名}`（域名以 §5.7 为准：缺省取配置区「默认邮箱域名」），标注【已推导】。
2. 推导不出（学号也缺失/格式错误）→ 进异常清单，注明"缺少邮箱，无法接收邮件"，从收件人串排除但留在组内名单。
3. **表无邮箱列但有微信列（微信号/手机号）** → 全员按学号推导邮箱进收件人串；微信列仅作联系信息保留在名单与内部记录，**不进收件人串**（微信不是邮件地址）。

### 6.3 筛选执行

1. 按 Q1 收集的规则执行；被筛除者进入"致谢组"，摘要计入"未通过筛选"。
2. **规则与数据冲突**（规则提到某列但表中无此列）：不猜测 → 同一轮内提问澄清；用户无法回答时降级处理（按报名时间排序并标注"该列缺失，已降级处理"），结果文档"待确认事项"中记录。
3. **多文件列结构不一致**（A 文件有身份列、B 文件没有）：规则依赖字段缺失的记录**不剔除**，保留进入判定阶段，进异常清单（"身份缺失，无法判定"），待确认事项列出；判定时按最保守语义（不因字段缺失而落选录取）。摘要注明受影响记录数。

### 6.4 状态判定

1. 按 Q2 收集的规则执行 → 录取组 / 候补组 / 未录取组。
2. 排序：报名时间缺失者排最后；**时间并列 → 学号升序**（tie-breaker，保证可复现）。
3. 未录取组 + 未通过筛选者 → 合并为"致谢组"。
4. 模式C（仅通知不录取）：按排序依据顺序**连续均分**为 N 组（各组人数差 ≤1），不区分录取/候补；致谢组 = 未通过筛选者。模式B：全部录取，无候补组。

### 6.5 分组与收件人串

1. 每组列出完整成员：姓名、学号、邮箱（含【已推导】标注）、联系方式（微信/手机，有则列出，无则略）、判定理由。
2. 收件人串：组内全部成员邮箱，英文分号 `;` 分隔、不加空格、结尾无多余分号。无法推导邮箱者留在组内名单、不出现在收件人串，摘要注明各组"组员数/有邮箱可接收数"。
3. 抄送串：以 Q4 收集值为准（分组填写时按录取/候补/致谢分别对应）；未提供 → 留空并列「待确认」，不编造抄送地址；本次未申请邮件写作 → 各组抄送位留空，不列入待确认。

## 7. 阶段五：统一输出（工作日志存档）

### 7.1 对话简报（固定模板，数值【此为例】；数字一律从名单计数得出，与文档「1. 统计摘要」逐项同源）

```
✅ 筛选完成（共处理 87 人【此为例】，去重后 83 人）
- 录取 30 人 ｜ 候补 10 人 ｜ 致谢 43 人（其中未通过筛选 7 人）【此为例】
- 推导邮箱 5 个 ｜ 异常记录 3 条 ｜ 待确认事项 1 条【此为例】
- 规则来源：本次设置（或 使用了缺省值）
- 结果已存档：筛选结果-{活动名}-{日期}.txt（环境无法输出文件时改为：结果已以对话 Markdown 交付，建议文件名：筛选结果-{活动名}-{日期}.txt）
请核对分组名单，确认后生成各组邮件正文。（本次未申请邮件写作时改为：请核对分组名单，确认后交付完成。）
```

### 7.2 筛选结果文档（工作日志存档机制）

- **命名规则：** `筛选结果-{活动名}-{YYYY-MM-DD}.txt`（内容为 markdown 格式）；活动名缺失 → `筛选结果-{YYYY-MM-DD}.txt`；**同日多次运行 → 追加序号 `-2`、`-3`**（按 (活动名, 日期) 配对计数，不同活动同日互不影响；新运行绝不覆盖既有文件）。文件名非法字符（`\ / : * ? " < > |`）替换为 `-`。
- **工作日志性质：** 每次运行的文件**不覆盖、自动保留**，构成组织筛选工作日志——历史活动的结果、规则、名单全程可查、可追溯；旧文件清理由用户手动删除。
- 文档内容结构（逐字）：

```markdown
# 报名筛选结果（活动名 · 日期）

## 1. 统计摘要
总人数、去重后人数、录取、候补、致谢、未通过筛选、重复报名、异常记录、
推导邮箱数、各组"组员数/有邮箱可接收数"、去重依据（报名时间列/行序）、
未识别列（若有）、名额未满/超出说明（若有）、规则来源（本次设置/缺省值）、
本次所用筛选规则与判定规则原文

## 2. 内部记录（完整名单 · 工作日志主体）
表格：排名（可选，按最终排序） | 姓名 | 学号 | 邮箱 | 联系方式（微信/手机，有则列出） | 报名时间 | 状态 | 备注（【已推导】/重复/异常等）

## 3. 分组名单（供邮件正文生成使用）
### 3.1 录取组（N 人，N 从名单计数得出，与简报同源）
成员清单（姓名、学号、邮箱、联系方式（微信/手机，有则列出）、判定理由）
收件人串：（代码块，英文分号分隔完整邮箱串）
抄送串：（Q4 收集值；未提供则留空并列入待确认）
### 3.2 候补组（同上格式）
### 3.3 致谢组（同上格式）

## 4. 异常清单（⚠️ 高危异常置顶，如邮箱推导疑似错误）
每条：姓名/学号、问题描述、建议处理方式、高危标记

## 5. 待确认事项（如无则省略；含邮件要素缺失项；未申请邮件写作时邮件要素不列入）
逐项列出：规则冲突/降级处理、字段缺失、Q4 未提供的邮件要素等（仅申请邮件写作时）

## 6. 邮件要素（Q4 收集，供邮件正文使用）
活动名称、活动时间、活动地点、主题一句话、报名链接、报名截止、联系人、抄送、邮箱域名
（各项标注来源：用户提供 / 缺省值 / 待确认；本次未申请邮件写作时，本节仅写"（本次未申请邮件写作，邮件要素未收集）"）
```

- 模式C：第 3 节标题改为 "3.1 通知第1组 … 3.N 通知第N组"（分组数=1 时 "3.1 通知组"），每组同格式，各组标题同样带人数；模式B：3.2 候补组标题下写"（无，全员录取模式）"。简报措辞相应调整。

### 7.3 异常清单与输出纪律

- 异常清单支持**文件级条目**（CSV 三类风险等）：姓名/学号 填"（文件级：CSV 日期文本化）"等；字段内逗号导致的列数错位按行级异常逐行记录；BOM 自动剥离并在摘要注明。文件级条目同样适用"⚠️ 高危置顶"。
- 简报 + 文件写入一次完成，不做"先看看效果"式的预览；对话输出 = 简报模板 + 一行文件路径，禁止追加自由解释。（本句仅约束筛选结果环节；阶段六的邮件正文属规格规定的交付物，按 §13 交付结构输出，不受此约束。）
- 用户要求调整（如把某人移到候补）→ **视为同一轮的输出修正**：接受修改，重新生成完整文档（修改原因记入内部记录备注），覆盖同名文件（不产生新序号），对话中仅重发更新后的简报，不展示 diff。
- 数据量 > 500 行时，简报与文档加注"数据量大，请抽查核对"。
- 规则来源两态：`本次设置 / 缺省值（标注"使用了缺省值"）`（每次运行均按本次回答重置，无沿用态）。

### 7.4 文件输出能力检查与降级（txt → 对话 Markdown）

**为什么**：部分宿主环境（如第三方集成）不支持技能创建/写入文件；此时不得假装已存档，也不得丢弃交付——**降级为对话 Markdown 完整交付，内容优先于形式**（与 §13.1.2"不假装已生成文件"的能力边界规则一致）。

1. **输出前检查**（§13.1.1「文件处理」项）：确认当前环境能否创建并写入文件（文件工具可用且实际可写）。无法确认 → 先尝试写入；**写入失败 → 重试一次 → 仍失败 → 降级**。
2. **降级交付方式**：把完整结果文档（§7.2 全部章节：标题 / 统计摘要 / 内部记录 / 分组名单 / 收件人串 / 异常清单 / 待确认事项 / 邮件要素）以对话 Markdown 原样输出（用代码块包裹，便于整体复制保存）；简报照常展示，并注明"环境无法输出文件，结果已以对话 Markdown 交付；建议文件名：筛选结果-{活动名}-{YYYY-MM-DD}.txt"。
3. **如实标注**：降级原因写入简报与「状态检查」（文件生成：当前环境不支持）；**不得声称"结果已存档"**。
4. **工作日志影响**：未落盘 = 本次结果不进入工作日志存档；向用户说明可自行将对话内容保存为文件（不自动补写、不伪造存档）。
5. 降级不影响后续流程：阶段六邮件正文本就默认对话 Markdown 交付（§8 第 4 步）。

## 8. 阶段六：确认与交付

1. **确认分组**：简报已展示、用户确认分组无误（无响应/"可以/随便/确认" → 视为确认）→ 进入邮件生成。用户提出调整 → 按 §7.3 输出修正机制重新生成完整文档、重发简报，再确认后进入下一步。

2. **逐组生成邮件正文**（对话 Markdown，含主题行，可复制；**一次交付全部组**，组间以分隔标题标明，如 `### 录取组邮件`）。**若 Q4 表明本次不需要邮件写作 → 跳过本步及第 3、4 步，确认分组后直接交付（简报 + 结果文档即最终交付物），流程结束。**
   - 组别 → 内容类型映射：录取组 = 录取/邀请通知；候补组 = 候补通知（说明候补含义与后续安排）；致谢组 = 致谢邮件；模式C = 各组通知正文；用户另提需求（紧急改期/取消/截止提醒/群通知）→ 对应内容类型（§13.5）。
   - 每封结构：主题行 → 称呼 → 正文（信息层事实 + 表达层语气）→ 落款（配置区「组织名称」）。
   - 按 §13 子流程执行：13.3 信息层核对 → 13.4 表达层调教 → 起草 → 13.7 终检。
   - 用户可要求单独改写某封 → 触发「本次变更」（§13.7），只重发该封，不重发全部。

3. **附内部区块**（置于全部邮件之后，顺序同 §13.7）：
   - `状态检查`：文案生成 / 文件生成 / 链接验证 / 识图 / 视觉检查逐项如实标注；
   - `发布状态`：**逐封标注**（草稿 / 待确认后发布 / 可发布）——Q4 有缺失要素的组一律 `待确认后发布`；
   - `待确认`：逐项列出缺失/冲突/未验证要素（含 Q4 未提供项），不得使用"XX等/其他信息"模糊字样；
   - `本次变更`：仅修改既有邮件时出现。

4. **文件生成（仅用户明确要求）**：按用户指定格式（Word DOCX / HTML 邮件 / PDF / TXT）生成；DOCX 用 docx 技能并执行渲染检查；文件动作在「状态检查」如实标注；默认对话 Markdown 不生成文件；筛选结果 txt 不写入邮件正文。

5. **边界声明**：公众号/海报/朋友圈/双语/多渠道内容包不在本技能范围。

6. **组织身份**：以「唯一配置区」为准；用户明确指定其他组织或要求去除组织品牌时，以用户为准。

## 9. 缺省值表（用户无响应/回答"随便"时的兜底）

任何问题用户答"随便 / 按默认 / 不答 / 跳过"即命中该题缺省值，**绝不追问**；其他题继续按顺序问完。以下默认值均为示例【此为例】。邮件要素类（Q4）缺省语义另见下——缺省值兜底的是"不追问"，不是"编造事实"：正文照常起草，缺失要素在交付区块 `待确认` 逐项列出，发布状态一律降为 `待确认后发布`；**Q4 整题答"无"或未填 = 本次不需要邮件写作**（见 Q4 整题行）。

| 问题 | 默认值 |
|---|---|
| Q0 多 sheet | 使用第一个含数据的 sheet，摘要注明 |
| Q0 数据问题 | 仅报告，不自动删除任何记录（宁报告不丢弃） |
| Q1 筛选规则 | 不筛选（全员进入判定），摘要注明"本次未设置筛选规则" |
| Q1 类型3 优先级 | 单个字母 = 仅该项优先，其余按报名时间；未提供 = 跳过该维度，仅按时间排序 |
| Q2 判定模式 | 模式A：录取 30 人、候补 10 人【此为例】 |
| Q2 排序依据 | 按报名时间先后；并列按学号升序 |
| Q3 筛选标准 | 会话内重建：按本次回答设置本次标准（不沿用、不落盘） |
| 活动名 | 缺失时结果用日期命名 |
| Q4 整题（答"无"或整题未填） | **本次不需要邮件写作**：跳过邮件要素收集与阶段六邮件生成，仅交付筛选结果；部分填写时未填项按下列逐项缺省 |
| Q4 活动时间 / 地点 / 报名链接 / 报名截止 / 联系人 | **无缺省值 → 标记待确认**：正文该要素位写占位提示（不编造），发布状态=待确认后发布，不追问 |
| Q4 主题一句话 | 用活动名称；活动名也缺 → 待确认 |
| Q4 抄送 | 无抄送（抄送串留空，不编造地址） |
| Q4 邮箱域名 | 配置区「默认邮箱域名」 |

## 10. 内置规则库（P3 的完整定义；示例参数均为示意【此为例】）

### 10.1 筛选规则 · 内置类型库（Q1，界面选项 1–5 与类型编号一一对应）

| 编号 | 类型 | 说明 | 需填参数 |
|---|---|---|---|
| 类型1 | **按报名时间先后**（先到先得） | 按报名时间排序处理，通常用于名额有限的活动 | 无 |
| 类型2 | **按列取值筛选** | 从表中可筛列选一列，保留或剔除指定取值（两方向等价；如性别保留女【此为例】） | 列字母 + 取值字母（`!` 前缀 = 剔除；选项来自表格可筛列与取值） |
| 类型3 | **按列优先级** | 从表中可筛列选一列，按指定取值顺序优先，同优先级内按时间排序；单个字母 = 仅该项优先，其余按默认排序 | 列字母 + 优先级字母序列或单字母 |
| 类型4 | **组合筛选** | 上述类型按用户声明顺序执行，前者筛除者不进入后者 | 组合中每个类型的参数 |
| 类型5 | **自定义** | 自然语言描述，原样采纳并回存 | 规则文本 |

**组合规则：** 多个类型按"用户声明顺序"（数字出现顺序）先后执行；摘要中按序记录执行链（如"类型2 → 类型3 → 时间排序"）。
**说明：** 本库为按真实表格泛化的执行版（v2.1）；早期版本的类型库（身份/书院/年级限定，编号 1–6）仅供追溯。

### 10.2 状态判定规则 · 内置模式库（Q2）

| 编号 | 模式 | 说明 | 需填参数 |
|---|---|---|---|
| 模式A | **名额制** | 录取 N 人、候补 M 人，按排序依据依次落位，其余致谢 | 录取人数 N、候补人数 M（可 0）、排序依据 |
| 模式B | **全员录取** | 通过筛选者全部录取，无候补 | 无 |
| 模式C | **仅通知不录取** | 不区分录取/候补，按筛选结果分组通知（如活动通知类） | 分组数 |
| 模式D | **自定义** | 自然语言描述（如"优先大二及以上，同优先级内按时间"） | 规则文本 |

**排序依据参数：** 时间先后 / 按列优先级 / 组合（默认时间先后；并列一律学号升序 tie-breaker）。

## 11. 示例标注与防错清单

- **【此为例】纪律**：本文档与标准模板中的所有示例值（人数、书院名、日期、域名）均为示意，不代表真实标准；真实标准以 grilling 中用户的选择为准。示例标注只约束模板与说明文字，**不约束渲染出的真实数据**（Q1 选项的真实人数、简报真实数字均不标注）。把筛选标准落盘为文件（筛选标准.md）→ 禁止，标准仅会话内存 + 随结果文档落盘原文。**§13 邮件模板同样适用**：模板示例中的时间/地点/链接/人数/主题均为示意【此为例】，真实要素一律来自 Q4 与用户提供；句式与结构（称呼、开场、落款格式、语气规则）是格式规范可保留执行，具体事实值必须标注【此为例】。**唯一配置区除外**：配置区取值为规则默认值，非示例，换组织改写后即成为真实默认值。
- **防错倾向清单**（出现即纠偏）：
  - 先预览再确认 → 禁止，P2 只存储不产出。
  - 静默改字段（如把"张三三"改"张三"）→ 禁止，强制要求 4。
  - 中文分号 `；` → 禁止，强制要求 1。
  - 统计数字心算 → 禁止，一律从名单计数。
  - 多轮追问同一问题 → 禁止，缺省值表兜底。
  - 把示例值当真实规则执行 → 禁止，【此为例】纪律。
  - 把数字编码当真实取值（见到 1/2/3 不追问含义就直接执行）→ 禁止，编码列必须请用户口述含义并记入规则原文。
  - 把示例/样本列名当真实列名（如拿 references 示例中的列名去真实表找列）→ 禁止，不预设表格模板，一切以实际表头识别结果为准。
  - 把 Q4 未提供要素写进邮件正文（编造时间/地点/链接）→ 禁止，强制要求 6。
  - 把缺省"待确认"当真实事实（如把占位时间当已确认时间写进正文）→ 禁止，强制要求 6。
  - 对话交付却声称已生成文件 → 禁止，强制要求 7（状态检查如实标注）。
  - 把群通知写成公众号/朋友圈风格 → 禁止，§13.5 群通知类型 + 边界声明。
  - 环境无法写文件却声称"已存档" → 禁止，§7.4 降级对话交付并如实标注。

## 12. 文件说明

| 文件 | 作用 |
|---|---|
| SKILL.md（本文件） | 执行版指令，技能运行以本文件为准；**内置规则库（§10）与 Q1/Q2 界面为按真实表格泛化的执行版（类型 1–5）**（历史类型库 1–6 仅供追溯）；**Q3 标准存储为会话内机制（每次按本次表格与回答重建、不落盘）**；含文首「唯一配置区」与 §13 邮件与群通知写作模块 |
| 筛选结果-{活动名}-{日期}.txt | 本技能输出（运行时工作目录，工作日志性质不覆盖；含本次筛选规则、判定规则原文与邮件要素区），供 §8 确认后生成各组邮件正文；环境无法输出文件时降级为对话 Markdown 完整交付（§7.4） |
| references/输入解析与边界示例.md | Q1/Q2 输入解析算法的正反例表，解析不确定时查阅 |

## 13. 邮件与群通知写作（仅邮件 + 群通知范围）

### 13.0 适用时机与执行顺序

**触发**：阶段六确认分组后自动进入（Q4 表明本次不需要邮件写作时跳过）；或「仅邮件写作」降级模式（§3 第三分支）直接进入。

每次写作严格按以下顺序执行：

1. **能力状态检查**（13.1）：识别任务需要的能力，并确认当前环境是否具备。
2. **任务识别**（13.2）：确定内容类型、渠道、语言、交付格式和是否需要修改已有文件。
3. **信息层**（13.3）：提取、核对并保留用户提供的事实（含 Q4 收集的邮件要素）；不要先写文案再补核对。
4. **表达层**（13.4）：根据活动类型、受众和渠道选择结构、语气和信息密度。
5. **交付与检查**（13.6/13.7）：按对话 Markdown 交付，执行发布前检查，附内部区块。

优先级固定为：**用户明确要求 > 当前任务事实 > 本技能规则 > 默认写作偏好（配置区）**。规则冲突时，保留事实准确性、用户授权边界和可发布性。

### 13.1 能力状态检查

在开始写作、验证链接、读取附件或创建文件前，先默默建立能力清单。不要把工具存在与否、理论上能做什么，与本次环境实际可执行混为一谈。

#### 13.1.1 Check these capabilities

- **文本处理**：能否读取用户文字、提取事实、生成和修改文案。
- **联网访问**：能否访问公开网页、查询外部资料或验证 URL。
- **浏览器操控**：能否打开网页、点击、输入、截图或读取页面状态。
- **图像理解**：能否读取用户提供的图片、海报、二维码、截图或扫描件中的信息。
- **文件处理**：能否创建、读取、修改和保存用户选择的 DOCX、PDF、HTML、Markdown 或 TXT。
- **视觉渲染检查**：能否将文档/PDF渲染为页面图片并实际检查排版。

#### 13.1.2 Capability boundary rules

- 当前环境没有联网能力时，不访问网页、不声称链接可用、不补充外部事实；把需要联网的工作列入 `状态检查`。
- 当前环境没有浏览器操控能力时，不声称已打开、点击、提交、截图或验证页面；链接只能标记为"未验证"。
- 当前环境无法识图时，不读取或猜测图片、海报、二维码、截图或扫描件内容；要求用户提供文字或可读文件，并列入 `状态检查`。
- 当前环境不能创建或修改用户要求的文件格式时，不假装已生成文件；在能力范围内提供对话文本，并列出无法完成的文件动作。
- 当前环境不能做视觉渲染检查时，不声称"已通过视觉检查"；在 `状态检查` 中标注"未完成视觉检查"。
- 任务中只有一部分超出能力范围时，继续完成可完成的部分；不得因一个缺口虚构其他检查结果。
- 不要为了完成能力外的工作而自行安装软件、登录账号、绕过权限或扩大外部操作范围，除非用户明确授权且环境支持。

#### 13.1.3 Status Check output

交付结果中加入简短的内部区块 `状态检查`，放在正文之后，不放进可直接发布的正文。至少说明：

```text
状态检查：
- 文案生成：已完成 / 未完成
- 文件生成：已完成 / 未请求 / 当前环境不支持
- 网页或链接验证：已完成 / 未请求 / 当前环境无联网或浏览器能力，未验证
- 图片、二维码或附件读取：已完成 / 未请求 / 当前环境无法识图，未读取
- 文档视觉检查：已完成 / 未请求 / 当前环境不支持，未检查
```

只列与本次任务有关的能力。`状态检查` 不替代 `待确认`：前者说明工具和执行能力，后者说明活动信息或发布材料缺口。

### 13.2 任务识别

先判断以下字段；无法从用户请求确定的字段，不要擅自假设：

- **内容类型**（仅本技能范围）：活动邀请、录取/入选通知、候补通知、致谢/未入选通知、群通知、报名/截止提醒、紧急改期/取消通知、招新/项目招募邮件、部门/运营组介绍邮件、活动回顾邮件。
- **渠道**：邮件、群聊（群通知）。
- **组织上下文**：以文首「唯一配置区」为准；仅当用户明确提供其他组织名称或明确要求去除组织品牌时切换组织上下文。
- **语言**：中文为主；用户要求时可中英双语标题（双语内容包不在本技能范围）。
- **交付格式**：默认对话 Markdown；用户明确指定时 Word DOCX / HTML 邮件 / PDF / TXT（§13.6）。
- **修改关系**：新建内容，还是修改已交付的同一份邮件/文件（触发「本次变更」）。

**不询问"以哪种格式交付"**：默认对话 Markdown（§8 第 4 步：用户明确要文件才生成文件）。

### 13.3 信息层

从用户输入与 Q4 中提取并核对：活动/项目名称、主办方、目标、主题、嘉宾及职务、时间、地点或线上链接、面向对象、报名方式、报名链接、截止时间、费用、名额、参与要求、联系人、合作方、附件/指引和目标渠道。缺失要素在 Q4 已标注待确认的，正文该要素位写占位提示，不编造。

#### 13.3.1 Source-of-truth rules

- 用户提供的事实是唯一事实源。不得发明嘉宾、奖项、合作方、名额、URL、地点、日期、费用、截止时间或成果。
- 在默认组织上下文（配置区）中，若用户没有提供主办方，可使用配置区「组织名称」作为组织署名和默认主办方；这只是组织身份默认值，不代表可以补写未提供的导师、合作方、联系方式、名额或成果。
- 不把常识、当前日期、搜索结果或另一版本的内容默认为用户确认的信息。
- 用户只给出模糊想法时，可以生成草稿，但要明确缺失字段；不得把草稿标为 `可发布`。
- 修改已有文案时，保留未被要求修改的准确事实；比较旧版和新版，记录已改变的公开事实。

#### 13.3.2 Date and time check

- 核对日期与星期是否一致。
- 年份缺失时，不根据当前年份自行推断。
- 核对开始时间、结束时间、报名截止时间及各处重复信息是否前后一致、顺序合理。
- 出现 **日期与星期不一致、年份缺失、时间前后矛盾** 时，保留用户原文并逐项列入 `待确认`，不得静默修正。

#### 13.3.3 Link, asset, authorization, and claim check

- 报名链接必须完整；占位符、截断链接或未提供的链接列入 `待确认`。
- 只有在联网和浏览器能力均可用、且实际完成安全访问时，才能说链接已验证；不提交表单、不登录、不绕过权限。
- 文案提到二维码、附件、海报、地图或地点指引而用户未提供时，明确列出缺失材料；有帮助但非必需的补充材料标注 `非必要`。
- 检查公开联系方式是否完整、适合公开。
- 照片、可识别个人信息、嘉宾资料、合作方/赞助方名称需要公开授权；不得假定已获授权。
- 识别"保证就业""一定获奖""官方合作""独家""百分之百录用"等未经证实的承诺。保留有明确依据且已确认的表述，否则改为准确措辞并列入 `待确认`。

### 13.4 表达层（核心语言调教）

先保证事实准确、信息完整、方便复制，再决定氛围和修辞。文案应让同学一眼看懂，负责人少改或不用改。

#### 13.4.1 Tone calibration

- **群通知、报名提醒、截止提醒、事务公告**：直接、简洁、可执行，少修饰。
- **音乐会、展览、讲座、典礼、沙龙、书院和文化艺术活动**：正式、克制、有邀请感；允许与活动相关的适度文雅和氛围。
- **室内或室外趣味活动**：活泼、灵动、动态；可以偶尔以反问开头，但不要每次都反问。
- **招新、志愿者招募、部门介绍**：亲切、具体、实用，避免公司口号和过度文学化。
- **活动回顾**：以已确认的事实和现场细节为主，可适度感谢和表达情绪，不空泛煽情。

"接地气"指符合真实校园场景、容易理解、组织者愿意直接使用，不等于全部口语化或行政化。正式文化活动可以保留仪式感、邀请感和与音乐、艺术、记忆、交流、陪伴、共同体验相关的意象；不得添加无关比喻、拟人、品牌 slogan 或空泛抒情。

#### 13.4.2 Style and rhythm

- 使用学生干部、宣传部和活动负责人日常能直接采用的清楚表达。
- 通过具体动词、丰富但熟悉的词汇和变化的句式提升表达，不以增加文学感为目标。
- 少用"首先、其次、最后"和机械的"不是……而是……"。避免连续相同的主谓宾句、重复排比和过密反问。
- 允许句长有变化，但不要堆叠长句、制造晦涩表达或重复信息。

#### 13.4.3 Information presentation

- Markdown、HTML、DOCX 等富文本中，可对活动名称、主题、嘉宾、时间、地点和行动提示选择性加粗；不要把每句话都加粗。
- DOCX、HTML 和其他富文本默认将主标题居中；普通章节、正文和项目卡片标题左对齐。
- 正式讲座、正式邀请和典礼可将主讲人、时间、地点等放入单独的可扫读信息区块。
- 娱乐性活动、演出和展览将关键信息融入正文，并在支持富文本时加粗时间和地点，不强行增加行政式列表。

### 13.5 内容类型与场景格式

#### 13.5.1 正式邀请/录取邮件

依次包含：邮件主题、称呼、简洁而有氛围的开头、活动/主题/主办方/参加理由、嘉宾或亮点、适合场景的信息呈现、温和邀请、主办方落款（配置区「组织名称」）。缺失信息不放进正式正文，正文之后列 `待确认`。

#### 13.5.2 学术讲座邮件

当用户提供主讲人或主持人背景时，尽量详细介绍其当前职务、单位、教育经历、曾任职务、研究方向、论文或专著；只能使用用户提供的事实，人物介绍最长不超过用户提供的背景介绍。未提供背景时，只写已确认的姓名、职称和单位。

#### 13.5.3 招新/项目招募邮件

组织语境以「唯一配置区」为准（组织名称/全称/定位理念/常用术语）。项目合集或多任务内容：先给总览，再为每一个项目/任务建立独立表格卡片；不要把多个项目挤成一列长段落。卡片使用一致的字段、列宽、标签和间距，只放已提供的信息。DOCX/HTML 使用独立表格选项卡式卡片；Markdown 使用分开的标题和小表格。不得根据组织定位推断未提供的导师、合作方、技术栈、名额、职业结果或项目成果。

#### 13.5.4 群通知（新增内容类型）

直接、简洁、可执行（语气基线见 13.4.1）。场景：紧急改期/取消、报名/截止提醒、事务公告、模式C 通知组、用户明确要求的群发通知。

- 结构：一行主旨 → 关键信息（时间/地点/链接/截止，加粗或独立区块）→ 行动要求 → 落款（配置区「组织名称」）。
- 与邮件的差异：更短，一句一段，便于群里扫读；不写称呼、不用长开头。
- 要素缺失同样走待确认，不编造。
- **群通知 ≠ 公众号/朋友圈**：群通知属本技能范围，公众号/朋友圈/海报等内容包不在范围（边界声明）。

#### 13.5.5 活动回顾邮件

活动回顾必须以已确认的活动事实和现场细节为基础。除必要缺口外，以下有助于丰富内容但非必需的材料也要列入 `待确认` 并标注 `非必要`：演出曲目、现场照片、观众互动、观众感受、现场数据、后续安排等。不得用"XX等""其他信息"掩盖缺口。

#### 13.5.6 组织特化（绑定「唯一配置区」，以配置区为准）

- **组织理念、语气基线、常用术语**：见配置区对应字段，不在此重复全文；只有用户提供了对应环节（如 Demo/成果展示），才写入具体文案；可以强调真实项目、团队协作、教授指导、Workshop、Demo、项目成果沉淀和成长经历；不得把"有机会""优先考虑"改写成保证结果；技术词保留其自然的英文形式，首次面向非技术受众使用时补一个简短中文解释，不为显得专业而堆叠缩写。（组织特化，以配置区为准；换组织时改写）
- **版式与配色**：仅在 DOCX、PDF、HTML 或其他支持样式的交付物中使用配置区「视觉配色 Token」；对话 Markdown 只保留层级、加粗和独立项目卡片的结构，不伪造颜色。视觉感应应稳定、清晰、偏技术和校园协作，不使用紫色渐变、霓虹大面积背景、装饰性光晕或未经提供的 logo/品牌图形。主标题默认居中；邮件主题使用浅蓝灰信息条；正文保持左对齐和充足留白。两个及以上项目或任务使用独立卡片：主色标题/编号区 + 信息区背景信息区 + 宽内容列，卡片之间留出明显垂直间距。项目卡片字段优先使用：项目名称、方向/标签、项目定位、周期、核心任务、招募人数、适合背景、参与方式、申请方式；没有提供的字段不留空占位，也不自行补齐。邮件、HTML 和 PDF 中的链接使用强调色；DOCX 表格避免固定行高，让长文本自然换行。版式是组织的默认建议，不是事实来源；用户指定模板、学校官方视觉规范或其他品牌系统时，以用户要求为准。

### 13.6 输出与文件规则

- **对话 Markdown（默认）**：直接在对话中输出（含主题行/称呼/正文/落款），保留 Markdown 加粗和标题，不创建文件。
- **Word DOCX**（用户明确要求）：生成可编辑桌面文档；用 `docx` 技能创建/修改，并执行渲染和 PNG 视觉检查。
- **PDF**（用户明确要求）：生成固定版式文件；优先从已验证的 DOCX 或稳定文档源生成，并渲染页面检查。
- **HTML 邮件**（用户明确要求）：保留标题、加粗、链接和信息区块为原生 HTML。
- **Markdown 文件/TXT**（用户明确要求）：TXT 不保留加粗、表格或颜色。

文件默认保存至用户桌面。用户要求修改同一封邮件时，原地更新最初创建的文件；不得新建副本。若原始文件正被 Word 或其他程序占用，不强制关闭程序、不覆盖实时编辑，明确说明阻塞并在能力范围内交付其他格式。

对话交付的邮件正文不写入筛选结果 txt（§8 第 4 步）。

### 13.7 发布状态与交付区块

#### 13.7.1 Release status

- `草稿`：首次生成、探索性版本或用户明确要求继续修改。
- `待确认后发布`：任何必要事实、日期、链接、材料、授权、渠道差异或双语差异未解决。
- `可发布`：相关能力检查和内容检查均完成，没有未解决的必要事项，也没有把未验证链接或未确认授权当作已确认事实。

#### 13.7.2 Required handoff blocks

交付顺序为：

1. 可直接发布的正文/文件。
2. `状态检查`：说明本次环境能力和哪些检查实际完成。
3. `发布状态`：草稿、待确认后发布或可发布（逐封标注）。
4. `待确认`：逐项列出缺失事实、冲突、未验证材料和需用户决定的事项；不得使用 `XX等`、`其他信息`、`相关事项` 等模糊字样。
5. `本次变更`：仅在修改已有邮件或多渠道内容时，列出改变的事实及受影响渠道。

`状态检查`、`发布状态`、`待确认` 和 `本次变更` 是内部交付信息；除非用户要求，不放入面向学生的正文。

#### 13.7.3 Final review checklist

交付前默默检查：

- 所有事实是否来自用户提供的信息（含 Q4）。
- 日期、星期、年份、时间顺序和截止时间是否一致。
- 渠道之间和邮件各组之间是否一致。
- 链接、二维码、附件、地点指引、联系方式和授权是否实际提供或已验证。
- 语气是否符合活动类型；正式活动没有被写成普通行政通知，也没有被过度口语化或文学化。
- 标题是否符合所选格式；富文本标题是否居中；多项目内容是否使用独立卡片。
- 是否存在夸大承诺、未经证实的合作关系或结果性表述。
- 所有必要缺口和非必要但有帮助的材料是否完整列入 `待确认`。
- 能力外任务是否已经在 `状态检查` 中明确标注，且没有被声称为已完成。

### 13.8 响应规则

- 默认以对话 Markdown 交付，不询问格式；用户明确指定格式时，按指定格式交付。
- 用户明确要求解释或检查 Skill 时，说明规则、限制和检查结果，不擅自生成无关文案。
- 用户要求生成文案时，少解释过程，直接给正文和必要的内部交付区块。
- 不重复询问已经确认的信息（Q4 已收集的要素不再问），不把内部状态写进公开正文，不虚构"已联网""已打开链接""已识图""已完成视觉检查"。
